[project]
name = "claude-zip-watcher"
version = "1.0.0"
description = "Automatically extracts zip files from a Google Drive watch folder"
requires-python = ">=3.11"
dependencies = ["watchdog>=4.0"]
license = "MIT"
